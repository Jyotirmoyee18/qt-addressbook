name: Qt Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v3

    - name: Install Qt
      run: sudo apt-get install qtbase5-dev qt5-qmake

    - name: Build
      run: |
        qmake
        make